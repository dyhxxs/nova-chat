# 安全策略

本文描述 Nova Chat 1.1.3 的安全边界。Nova Chat 是第三方自托管客户端，不是官方 ChatGPT/Codex 产品。

## 1. 秘密管理

### 服务器秘密

生产环境必须配置：

- `SERVER_MASTER_KEY`：至少 32 个随机字符，用于 AES-256-GCM 加密管理员保存的第三方 API Key。
- `ADMIN_BOOTSTRAP_TOKEN`：至少 24 个随机字符，只用于首次管理员初始化。
- 第三方 API Key：推荐由管理员在 App 管理控制台写入网关，不进入 APK。

禁止把这些值提交到 Git、写入移动端常量、发送到普通用户、打印到日志或放入截图。`SERVER_MASTER_KEY` 与数据库备份必须配套保存；丢失主密钥后，数据库内已有的第三方 API Key 无法恢复。

### 移动端秘密

- 登录 Session Token 保存到 Expo SecureStore。
- 高级直连模式的用户自有 API Key 也保存到 SecureStore。
- 对话历史和非秘密设置保存在本机应用数据中。
- 管理员服务商 Key不会通过管理员公开配置接口返回，只显示是否已设置和最后四位预览。

## 2. 认证与授权

- 密码使用随机盐和 `scrypt` 派生摘要。
- Session Token 使用 256-bit CSPRNG 生成，数据库只保存 SHA-256 哈希。
- Session 有有效期；退出登录会撤销当前 Session。
- 禁用用户会撤销该用户所有 Session，后续登录失败。
- 角色分为 `admin` 和 `user`。
- 只有管理员可读取用户列表、修改角色/状态、配置模型服务和测试服务商连接。
- 网关阻止禁用或降级最后一个启用的管理员。
- `APP_ACCESS_TOKEN` 仅用于旧版兼容；新部署应留空。Legacy 身份不能上传附件。

`REGISTRATION_ENABLED=false` 可关闭公开注册。若服务只供少量朋友使用，推荐初始化并创建所需账户后关闭注册。

## 3. 第三方 API Key 加密

网关使用 `SERVER_MASTER_KEY` 的 SHA-256 结果作为 AES-256-GCM 密钥，并为每次保存生成新的 96-bit IV。SQLite 只保存版本、IV、认证标签和密文。公开接口永不返回明文 Key。

该机制保护静态数据库文件不直接泄漏 Key，但不能防御已经获得服务器进程权限和主密钥的攻击者。生产服务器仍需要磁盘加密、最小权限、补丁管理和访问审计。

## 4. 附件安全

- 允许类型：JPEG、PNG、WebP、GIF、PDF。
- 默认单附件上限：25 MB；可通过 `MAX_FILE_BYTES` 调整。
- 网关同时检查声明 MIME 和真实文件头。
- 文件使用随机 UUID 作为存储名，不使用用户文件名作为磁盘路径。
- 下载时校验所有者；普通用户不能读取他人的附件，管理员可读取用于管理/排障。
- 上传响应不返回服务器路径、用户 ID 或内部创建信息。
- 文件名会过滤控制字符和危险路径字符。

文件头检查不是杀毒系统。若部署面向不受信任的公众，应额外接入恶意软件扫描、配额、生命周期清理和独立对象存储。

## 5. 网络安全

生产环境必须：

- 只通过 HTTPS/WSS 暴露 App API；
- 由 Caddy/Nginx/云负载均衡终止 TLS；
- 仅让反向代理访问本机 8787；
- 正确代理 WebSocket Upgrade；
- 配置服务器防火墙、备份和安全更新；
- 限制 `CORS_ORIGINS`（如果同时提供 Web 客户端）；
- 在第三方模型账户启用预算、限额和费用告警。

开发环境的明文 HTTP 只能在可信局域网或模拟器中临时使用。正式 App 构建应设置 `ALLOW_CLEARTEXT=false`。

## 6. 流式生成与资源控制

- REST 和 WebSocket 都要求认证。
- 每个用户应用滑动窗口限流和并发上限。
- WebSocket 有握手超时、最大消息大小、服务端 heartbeat 和客户端停滞检测。
- 取消请求会传播 `AbortSignal` 到服务商调用。
- 客户端不会在已经输出 partial 后自动重放请求，避免重复回复和重复计费。
- 历史长度、最大输出 token、上传大小均有服务端上限。
- 管理员维护模型 allowlist，普通用户不能任意选择未允许的高成本模型。

限流在单个网关进程内实现。如果横向扩展多个实例，应把限流/并发状态迁移到 Redis 等共享存储，并使用支持多实例的 Session 与文件存储设计。

## 7. 模型内容与提示注入

模型输出、网页搜索结果、PDF 和图片均视为不可信内容。身份提示可以降低错误自称其他模型的概率，但无法从数学上保证模型永不产生错误陈述。

不要让模型输出自动获得以下权限：

- 执行服务器 Shell；
- 读取任意服务器文件；
- 访问云凭据；
- 修改生产数据库；
- 代表用户付款或提交外部操作。

当前网关不会执行服务器命令。第三方 Responses 的 `code_interpreter` 若可用，执行环境由第三方服务商提供，其隔离、留存和计费策略需要单独审查。

## 8. 高级直连模式

高级直连模式适合单人调试，不适合管理员共享服务：

- 请求和用户自有 Key 从手机直接发往第三方服务商；
- Nova 网关无法统一限流、撤销、审计或隐藏该 Key；
- 网关附件 ID 无法直接用于第三方，因此当前不支持附件引用；
- 第三方服务商将直接看到设备网络信息和请求内容。

多人使用时应保持托管网关模式。

## 9. 日志与隐私

Fastify 日志会脱敏 Authorization、密码、初始化口令、服务商 API Key、消息正文和自定义指令。仍应限制日志访问和保留时间，并检查上游代理、APM、崩溃收集和云平台是否额外记录请求体或 Header。

隐私政策至少需要说明：

- 账户信息和登录时间；
- 对话、图片、PDF 会发送给自托管网关和所选第三方模型服务商；
- Web Search/Code Interpreter 可能由第三方处理；
- 数据保存位置、删除方式、备份周期和联系邮箱。

## 10. 备份与恢复

需要备份整个 Nova 数据卷，包括：

- `nova-chat.sqlite` 及 SQLite WAL/SHM；
- `uploads/`；
- 与该数据库匹配的 `SERVER_MASTER_KEY`。

推荐在停止写入或执行一致性快照后备份。恢复后验证管理员登录、服务商 Key 解密和附件下载。不要把生产备份复制到个人电脑或公开网盘。

## 11. APK/IPA 分发

安装包下载与模型网关是两条独立链路。正式分发应使用应用商店、TestFlight、企业分发或支持 HTTPS、HTTP Range 和 CDN 的对象存储。临时内网穿透/简易文件服务器容易造成下载到一半断开，不应用于正式发布。

发布时提供：

- 版本号和构建号；
- SHA-256；
- 签名验证结果；
- 官方下载域名；
- 更新说明。
- Android 覆盖安装包必须递增 `versionCode`；LAN 测试包使用独立 `.lan` 包名后缀，只允许在可信私有 Wi-Fi 上使用明文 HTTP。

## 12. 漏洞报告

请勿公开发布包含真实密钥、用户数据或可直接利用步骤的漏洞。报告应包含受影响版本、复现条件、影响范围和建议修复方式，并通过项目维护者的私密安全渠道提交。
